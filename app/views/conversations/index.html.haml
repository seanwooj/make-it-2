- @conversations.each do |conversation|
  - other_user = conversation.other_user(current_user)
  = link_to conversation.messageables.last.body, conversation
  = link_to other_user.full_name, user_path(other_user)